{
  "routes": [
    {
      "method": "GET",
      "path": "/personals",
      "handler": "personal.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personals/count",
      "handler": "personal.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personals/:id",
      "handler": "personal.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/personals",
      "handler": "personal.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/personals/:id",
      "handler": "personal.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/personals/:id",
      "handler": "personal.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
